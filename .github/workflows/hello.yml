name: Hello world!

on:
  push:
    branches:
      - main

jobs:
  hello_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: |
          echo "Hello world!"
      - name: Now it is
        run: |
          date
      - name: Directory contents
        run: |
          ls -l
